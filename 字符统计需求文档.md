# 树园工具导航：字符统计需求文档

## 1. 文档信息
- 文档类型：详细规格
- 功能名称：字符统计
- 所属项目：树园工具导航
- 目标版本：建议 `v0.3.6` 或 `v0.4.0` 之后接入
- 开发原则：**独立开发，不影响现有“文本比对”和“图片压缩”功能**

---

## 2. 背景与目标

当前树园工具导航已经具备：
- 在线文本差异比对
- 在线图片压缩

下一个计划开发的工具是「字符统计」。这个工具的目标不是做复杂文本分析，而是提供一个**轻量、准确、即时、本地运行**的文本统计面板，满足用户在日常写作、内容发布、字段校验、程序开发中的基础统计需求。

本功能首期重点围绕三种统计方式：
- 字符
- 字节
- 字符长度

这个功能需要与现有工具保持一致的产品气质：
- 纯前端、本地运行
- 输入即反馈
- 视觉简洁、结果清晰
- 不和已有工具耦合

---

## 3. 用户需求调研结论

结合当前项目定位，以及常见在线字数/字符统计工具的共性需求，字符统计工具的核心用户诉求可归纳为：

### 3.1 用户真正关心的不是“复杂分析”，而是“立即知道长度是否超标”
典型场景包括：
- 写标题、简介、产品描述时，确认是否超出平台限制
- 填数据库字段、接口字段时，确认是否超过长度或字节限制
- 处理中英文混排、空格、换行、emoji 时，确认不同口径下长度差异

### 3.2 用户需要“不同口径并排展示”，而不是只给一个数字
单一“字数”往往不够，因为用户常会遇到这些问题：
- 为什么看起来一样长，数据库却超限了？
- 为什么一个 emoji 只看见 1 个，但程序长度不是 1？
- 为什么中文和英文在“字节数”上差异这么大？

因此，产品需要把“字符 / 字节 / 字符长度”并列展示，并给出明确说明。

### 3.3 用户希望“输入即统计”，不想额外点按钮
相对文本比对这类需要触发的功能，字符统计更适合：
- 粘贴即统计
- 输入即更新
- 删除即更新

它应当是一个低摩擦、实时反馈型工具。

### 3.4 用户希望解释清楚，而不是只给结果
如果不解释“字符”和“字符长度”的区别，用户会误以为统计错误。

因此首期需求必须包含：
- 指标定义说明
- 示例说明
- 结果口径统一

---

## 4. 功能定位

### 4.1 一句话定位
一个用于**实时统计文本字符、字节和程序长度**的本地工具。

### 4.2 核心价值
- 帮用户快速判断文本是否符合平台/字段/程序限制
- 帮用户理解“看起来一样，但统计结果不同”的原因
- 作为树园工具导航中的基础文本类工具，和“文本比对”形成互补

---

## 5. 范围定义

## 5.1 本期必须支持
- 文本输入区
- 实时统计
- 三种统计结果：
  - 字符数
  - 字节数
  - 字符长度
- 清空文本
- 复制文本
- 本地处理，不上传
- 基础说明文案

## 5.2 本期建议支持
- 总行数统计
- 非空行数统计
- 空格数统计
- 中文字符数 / 英文字符数 / 数字数

说明：这部分不是本次核心，但实现成本低、用户感知强，建议作为增强项一起纳入 v1。

## 5.3 本期明确不做
- 敏感词分析
- 可读性分析
- 关键词密度分析
- 文本纠错
- 多编码切换（如 UTF-8 / GBK / UTF-16 多模式切换）
- 历史记录

说明：为保持工具轻量，首期应避免做成“文本分析大杂烩”。

---

## 6. 指标定义

这是本需求的关键部分，必须在产品和实现层统一。

### 6.1 字符
定义：用户肉眼看到的“文本字符数量”口径。

建议实现口径：
- 按 **Unicode code point** 统计
- 中英文、数字、标点都算 1 个字符
- 空格算字符
- 换行算字符
- emoji 视为 1 个字符

目的：
- 更符合用户“我看到几个字符”的直觉

### 6.2 字节
定义：文本以某种编码存储时占用的字节大小。

本期建议口径：
- **统一按 UTF-8 统计**

原因：
- 浏览器原生能力好实现
- 与现代 Web / 接口 / JSON / 数据传输场景最一致
- 避免首期引入 GBK / UTF-16 / UTF-32 多编码复杂度

说明文案建议：
- “字节数按 UTF-8 计算”

### 6.3 字符长度
定义：程序中的字符串长度。

本期建议口径：
- 按 **JavaScript `string.length`** 统计
- 即 UTF-16 code unit 长度

意义：
- 这能直接反映前端/JS 场景中的真实“程序长度”
- 与“字符数”形成对照，帮助用户理解 emoji、特殊字符等带来的差异

### 6.4 三者区别说明
产品内建议明确给出示例：

示例文本：`A中😀`
- 字符：3
- UTF-8 字节：8
- 字符长度：4

这能帮助用户立即理解：
- 中文、emoji 在字节数上更大
- emoji 在 JS 长度上可能占 2

---

## 7. 典型用户场景

### 场景 1：内容运营写标题
用户在写标题/简介时，希望知道：
- 当前写了多少字符
- 是否接近平台限制

### 场景 2：开发者校验字段长度
用户在设计接口、数据库字段时，希望知道：
- 这个字符串在 UTF-8 下占多少字节
- JS 侧长度是多少

### 场景 3：处理中英文混排与 emoji
用户在写昵称、文案、消息模板时，希望知道：
- 为什么看起来 10 个字，程序里可能不是 10

### 场景 4：快速清洗与重试
用户不断删改文本，希望统计面板即时更新，而不是每次都手动触发。

---

## 8. 页面与交互需求

## 8.1 页面结构
建议沿用现有工具风格，结构如下：

1. 工具标题区
- 标题：在线字符统计
- 副标题：支持字符、字节、字符长度实时统计
- 标签：本地处理 / 实时统计

2. 输入区
- 大文本输入框
- 支持粘贴 / 输入
- placeholder 示例文案

3. 操作区
- 清空文本
- 复制文本

4. 结果区
- 核心三指标卡片：
  - 字符
  - 字节（UTF-8）
  - 字符长度（JS）
- 增强指标区：
  - 总行数
  - 非空行数
  - 中文字符数
  - 英文字符数
  - 数字数
  - 空格数

5. 指标说明区
- 简短解释三种口径差异
- 给出一个示例

## 8.2 交互规则
- 输入框内容变化时，结果即时更新
- 空文本时所有统计归零
- 统计计算不依赖按钮触发
- 大文本输入时，统计反馈应保持流畅
- 粘贴超长文本时，不应有明显卡顿或阻塞

## 8.3 可用性要求
- 用户无需理解技术细节也能使用
- 用户若关心技术差异，可以通过说明区理解“为什么三个数字不同”

---

## 9. 功能规格

## 9.1 输入能力
- 支持直接输入
- 支持粘贴
- 支持保留原始换行
- 支持中英文、标点、emoji、空格、制表符

## 9.2 实时统计能力
- 输入事件触发重新计算
- 计算结果同步更新 UI
- 不需要点击“开始统计”

## 9.3 结果统计字段

### 核心字段
- 字符数
- 字节数（UTF-8）
- 字符长度（JS `string.length`）

### 增强字段
- 总行数
- 非空行数
- 中文字符数
- 英文字符数
- 数字数
- 空格数

## 9.4 辅助操作
- 清空文本
- 复制当前文本

---

## 10. 非功能需求

### 10.1 独立开发要求
- 必须作为第三个工具独立接入
- 目录建议：
  - `src/tools/char-count/`
- 不修改现有 `text-diff` 和 `image-compress` 的业务实现
- 仅允许修改公共壳层：
  - `src/app.js`
  - `index.html`（如确有必要）
  - `assets/app.css`（新增对应样式）

### 10.2 性能要求
- 普通文本输入应无感更新
- 大文本粘贴后统计应在可接受时间内完成
- 不引入重量级依赖库

### 10.3 隐私要求
- 全程本地运行
- 不上传文本
- 不接入第三方统计服务

### 10.4 可维护性要求
- 按现有工具模块规范拆分
- 至少包含：
  - `index.js`
  - `stats.js`
  - `utils.js`（如需要）

---

## 11. 文案建议

### 工具标题
在线字符统计

### 副标题
支持字符、字节、字符长度实时统计，全程在浏览器本地完成。

### 指标说明建议
- 字符：按用户可见字符统计
- 字节：按 UTF-8 编码后的存储大小统计
- 字符长度：按 JavaScript 字符串长度统计

---

## 12. 验收标准

### 功能验收
- 输入任意文本，结果即时更新
- 清空后所有数据归零
- 示例文本可正常填入
- 复制文本功能正常

### 统计验收
以下案例结果必须准确：

#### 案例 1
文本：`abc`
- 字符：3
- 字节：3
- 字符长度：3

#### 案例 2
文本：`中文`
- 字符：2
- 字节：6（UTF-8）
- 字符长度：2

#### 案例 3
文本：`😀`
- 字符：1
- 字节：4（UTF-8）
- 字符长度：2

#### 案例 4
文本：`A中😀`
- 字符：3
- 字节：8（UTF-8）
- 字符长度：4

#### 案例 5
文本包含多行、空格、制表符
- 行数与空格数统计准确
- 不因 CRLF / LF 差异造成错误

### 隔离性验收
- 文本比对功能不回归
- 图片压缩功能不回归
- 新工具仅通过壳层挂载接入

---

## 13. 后续扩展建议

### v2 可扩展方向
- 支持“字符数（含空格 / 不含空格）”切换
- 支持多编码字节统计（UTF-8 / GBK）
- 支持单词数、段落数、句子数
- 支持平台长度限制提示（微博、公众号、小红书、数据库字段）
- 支持结果复制

### 但不建议首期就做
原因：
- 首期目标是“把三种口径做准、做清楚、做顺手”
- 过早扩展会分散重点，增加解释成本和测试成本

---

## 14. 结论

「字符统计」是一个适合当前树园工具导航继续扩展的第三工具，原因如下：
- 和现有文本类能力相邻，用户理解成本低
- 需求明确，开发复杂度可控
- 纯前端可完成，符合当前项目技术路线
- 能补齐“文本处理基础工具”矩阵

首期建议：
- 聚焦“三个核心统计口径做准”
- 保持实时、简洁、本地处理
- 独立目录开发，不影响现有两个工具
