# 树园工具导航：完整功能清单 + 下一步 Roadmap

## 一、当前完整功能清单（已上线）

### 1) 站点与框架层
- 多工具壳层与工具切换（Hash 路由）
  - `#text-diff`
  - `#image-compress`
- 页面元信息动态切换（标题、副标题、工具名称、提示）
- 年份自动更新
- 本地静态运行（无需后端）
- 版本参数防缓存（模块 import 带 `?v=0.3.5`）

### 2) 文本差异比对（Text Diff）
- 支持中文 / 英文 / 代码 / 大文本粘贴
- 行级 diff（Myers 思路 + 邻接合并 replace）
- 差异分类
  - 内容差异（content）
  - 排版差异（format）
- 双栏编辑器
  - 左：原文本，右：对比文本
  - 行号层 + 差异高亮层
  - 差异行聚焦高亮
- 差异导航
  - 「开始比对」
  - 「上一处 / 下一处」
  - 点击下方差异行，自动定位到上方对应行
- 文本操作
  - 清空空行
  - 清空文本
  - 交换文本
  - 复制结果摘要
- 结果展示
  - 差异总数
  - 内容差异数 / 排版差异数
  - 双栏行号对照展示
- 稳定性处理
  - 处理 CRLF / LF 换行差异
  - 末尾空行口径处理
  - 大行数保护（防浏览器卡死）
- 文本比对历史（本地）
  - 自动保存最近记录（IndexedDB）
  - 恢复历史文本
  - 复制历史摘要
  - 删除单条 / 清空全部

### 3) 在线图片压缩（Image Compress）
- 上传方式
  - 点击选择
  - 拖拽上传
  - 支持批量
- 支持格式
  - JPG / PNG / WebP
- 压缩模式
  - 智能推荐
  - 高清优先
  - 极限压缩
  - 无损优先
- 参数控制
  - 输出格式（转 WebP / 保持原格式）
  - 质量滑杆
  - 最大边约束
- 结果与统计
  - 选择数量
  - 压缩前体积
  - 压缩后体积
  - 节省比例
- 下载能力
  - 单图下载
  - 全部打包下载（ZIP）
- 预览与列表
  - 文件列表状态
  - 预览区（前后关键信息）
- 图片压缩历史（本地）
  - 自动保存压缩结果
  - 刷新 / 删除 / 清空
  - 历史容量统计

### 4) 隐私与本地处理
- 文本比对全程本地运行
- 图片压缩全程本地运行
- 历史记录仅存当前浏览器本地（IndexedDB）
- 无服务端上传依赖

### 5) 测试与质量
- 已有自动化测试：
  - `diff-large.test.mjs`
  - `diff.test.mjs`
  - `summary.test.mjs`
  - `text-history.test.mjs`
  - `image-compress.test.mjs`
- 当前测试均可通过（本地 Node test）

---

## 二、下一步 Roadmap（建议按 4 周迭代）

## Phase 1（本周）：稳定性收敛与可观测性
目标：把“偶发对不齐 / 复现困难”降到可定位、可解释。

- 文本比对增加“调试开关”（仅开发态）
  - 输出 `dataset 行号 / 光标行号 / 定位行号` 三元组
  - 一键导出复现上下文（不含敏感正文，可做 hash 摘要）
- 行号一致性守卫
  - 点击定位后自动校验一致，不一致时 UI 提示“自动纠偏”
- 历史记录增强
  - 文本历史增加“来源时间、差异类型分布”
  - 图片历史增加“模式参数快照”（质量、最大边、输出格式）
- 体验小修
  - 高亮聚焦改为柔和淡入淡出（减少突兀感）
  - 导航跳转时可选“居中定位”与“保持上下文定位”

验收标准：
- 连续 20 次长文本定位，行号一致率 100%
- 滚动无明显闪烁，主观体验稳定

## Phase 2（下周）：文本比对专业化
目标：把“可用”升级到“专业可交付”。

- 差异粒度增强
  - 行内高亮（字符级/词级差异）
  - 忽略规则开关
    - 忽略首尾空格
    - 忽略连续空白
    - 忽略大小写
- 结果操作增强
  - 一键跳到“仅内容差异”或“仅排版差异”
  - 导出报告（TXT/Markdown）
- 性能优化
  - 超大文本分块与渐进渲染
  - 导航与渲染解耦，避免主线程长阻塞

验收标准：
- 5k~10k 行文本可在可接受时间内完成比对
- 结果导出可复用给评审流程

## Phase 3（第 3 周）：图片压缩专业化
目标：从“压缩工具”升级为“图片处理台”。

- 批处理策略增强
  - 按文件类型自动策略（PNG 走无损/近无损，JPG 走有损）
  - 失败重试与错误原因分类
- 输出能力增强
  - 保留/去除 EXIF 选项
  - 命名模板（原名+模式+时间）
  - 同步导出压缩报告（CSV）
- 预览增强
  - 放大镜对比
  - 前后分屏拖拽对比

验收标准：
- 100 张混合图片批处理稳定
- 平均压缩收益与质量可解释

## Phase 4（第 4 周）：平台化与第三工具上线
目标：从“两个工具”变成“可扩展工具平台”。

- 工具 SDK 化
  - 统一工具注册协议（元信息、挂载、生命周期）
  - 统一 toast / 日志 / 历史能力
- 上线第三工具（建议二选一）
  - 文本提取清洗（去重、去空行、格式化）
  - PDF 转图片 / 图片转 PDF（本地）
- 部署与发布
  - 版本日志页面
  - 发布检查清单（测试、回归、性能）

验收标准：
- 新工具接入时间 < 0.5 天
- 发布流程标准化，不靠口头记忆

---

## 三、优先级建议（从高到低）
1. 行号与定位稳定性（直接影响可信度）
2. 文本比对专业化（主功能护城河）
3. 图片压缩专业化（拓展使用场景）
4. 平台化与第三工具（规模化增长）

---

## 四、建议的版本里程碑
- `v0.3.6`：稳定性与定位一致性修复闭环
- `v0.4.0`：文本比对专业版（行内差异 + 忽略规则 + 导出）
- `v0.5.0`：图片压缩专业版（批处理策略 + 报告）
- `v1.0.0`：三工具平台化发布

